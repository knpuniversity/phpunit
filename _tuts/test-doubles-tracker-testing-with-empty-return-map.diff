diff --git a/src/AppBundle/Entity/Dinosaur.php b/src/AppBundle/Entity/Dinosaur.php
index 2c6b2ff..d0a3f52 100644
--- a/src/AppBundle/Entity/Dinosaur.php
+++ b/src/AppBundle/Entity/Dinosaur.php
@@ -82,6 +82,11 @@ class Dinosaur
         $this->tracker = $tracker;
     }
 
+    public function getTracker(): Tracker
+    {
+        return $this->tracker;
+    }
+
     public function track(): string
     {
         return sprintf(
diff --git a/src/AppBundle/Entity/Tracker.php b/src/AppBundle/Entity/Tracker.php
index 0c6095f..f6e014c 100644
--- a/src/AppBundle/Entity/Tracker.php
+++ b/src/AppBundle/Entity/Tracker.php
@@ -14,4 +14,10 @@ class Tracker
     {
         return random_int(1, 9);
     }
+
+    public function test($feature, $expected): string
+    {
+        // TODO: replace with real logic before pushing to production :)
+        return random_int(0, 1) ? 'yes' : 'no';
+    }
 }
diff --git a/tests/AppBundle/Entity/DinosaurTest.php b/tests/AppBundle/Entity/DinosaurTest.php
index 2480c71..bb136e3 100644
--- a/tests/AppBundle/Entity/DinosaurTest.php
+++ b/tests/AppBundle/Entity/DinosaurTest.php
@@ -40,4 +40,39 @@ class DinosaurTest extends TestCase
 
         $this->assertContains('area "5"', $dinosaur->track());
     }
+
+    /**
+     * @dataProvider trackerStatusProvider
+     */
+    public function testTrackerIsWorking(
+        string $expectedTestResult,
+        string $feature,
+        string $expectedStatus,
+        string $failureMessage
+    )
+    {
+        $tracker = $this->createMock(Tracker::class);
+        $map = [];
+
+        $tracker->method('test')
+            ->will($this->returnValueMap($map));
+
+        $dinosaur = new Dinosaur();
+        $dinosaur->addTracker($tracker);
+
+        $this->assertSame(
+            $expectedTestResult,
+            $dinosaur->getTracker()->test($feature, $expectedStatus),
+            $failureMessage
+        );
+    }
+
+    public function trackerStatusProvider()
+    {
+        return [
+            // expected test result, feature, expected status, failure message
+            'gps online' => ['no', 'gps', 'online', 'GPS should be online'],
+            'battery healthy' => ['yes', 'battery', 'healthy', 'Battery should be healthy'],
+        ];
+    }
 }
