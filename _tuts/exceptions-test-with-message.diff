diff --git a/src/AppBundle/Entity/Enclosure.php b/src/AppBundle/Entity/Enclosure.php
index f90314d..c5026cf 100644
--- a/src/AppBundle/Entity/Enclosure.php
+++ b/src/AppBundle/Entity/Enclosure.php
@@ -24,10 +24,17 @@ class Enclosure
      */
     private $isCarnivorous;
 
+    /**
+     * @var iterable
+     * @ORM\OneToMany(targetEntity="AppBundle\Entity\Security", mappedBy="enclosure", cascade={"persist"})
+     */
+    private $securities;
+
     public function __construct(bool $isCarnivorous = false)
     {
         $this->dinosaurs = new ArrayCollection();
         $this->isCarnivorous = $isCarnivorous;
+        $this->securities = new ArrayCollection();
     }
 
     public function getDinosaurs(): iterable
@@ -48,4 +55,9 @@ class Enclosure
     {
         return $this->isCarnivorous;
     }
+
+    public function addSecurity($security)
+    {
+        $this->securities[] = $security;
+    }
 }
diff --git a/tests/AppBundle/Entity/EnclosureTest.php b/tests/AppBundle/Entity/EnclosureTest.php
index 1f7f647..e7fd14d 100644
--- a/tests/AppBundle/Entity/EnclosureTest.php
+++ b/tests/AppBundle/Entity/EnclosureTest.php
@@ -47,4 +47,14 @@ class EnclosureTest extends TestCase
         $enclosure->addDinosaur(new Dinosaur('Velociraptor', true));
         $enclosure->addDinosaur(new Dinosaur());
     }
+
+    public function testItDoesNotAllowUnsafeSecurities()
+    {
+        $enclosure = new Enclosure();
+
+        $this->expectException(\InvalidArgumentException::class);
+        $this->expectExceptionMessage('This does not look safe');
+
+        $enclosure->addSecurity(new Dinosaur('Velociraptor', true));
+    }
 }
